# Distributed Integral Computation System

Система для распределенного вычисления определенного интеграла функции **f(x) = 1/ln(x)** методом средних прямоугольников. Проект разработан в рамках тестового задания для стажера на позицию Разработчик C++.

## 📌 Описание проекта

Приложение реализует распределенную вычислительную сеть:

- **Сервер (Server):** Формирует задачу, принимает ввод от пользователя (пределы и шаг) и распределяет нагрузку между подключенными узлами. Распределение происходит пропорционально количеству ядер CPU на каждом клиенте.
- **Клиент (Client):** Выступает в роли вычислительного узла. При подключении сообщает серверу свои ресурсы, а при получении задачи выполняет её в многопоточном режиме, используя `std::thread`.

## 🛠 Технологический стек

- **Язык:** C++17
- **Библиотеки:** Qt 6.10 (модули Core и Network)
- **Система сборки:** CMake 3.16+
- **Документация:** Doxygen style

## 🏗 Сборка и запуск

### Сборка через Qt Creator

1. Откройте файл `CMakeLists.txt` в корне проекта.
2. Выберите комплект сборки (Kit).
3. Нажмите кнопку **Build** (или `Ctrl+B`).

### Сборка из консоли (Windows)

Для корректной настройки окружения и путей к компилятору рекомендуется использовать **Qt Command Prompt**:

```bash
mkdir build
cd build
qt-cmake -G "MinGW Makefiles" ..
cmake --build .
```

## 🚀 Инструкция по использованию

1. **Запуск сервера:** Запустите исполняемый файл `Server.exe`. Он начнет прослушивание порта `1234`.
2. **Запуск клиентов:** Запустите один или несколько экземпляров `Client.exe`. В консоли сервера отобразится лог подключения и количество ядер каждого клиента.
3. **Ввод данных:** На стороне сервера следуйте подсказкам:
   - Введите **нижний предел** `a`.
   - Введите **верхний предел** `b`.
   - Введите **шаг интегрирования** (например, `0.0001`).
4. **Валидация:** Функция **1/ln(x)** имеет разрыв в точке **x = 1**. Система автоматически проверяет ввод и не позволит запустить вычисление, если интервал пересекает эту точку.
5. **Выполнение:** Нажмите `ENTER`. Сервер разделит задачу, разошлет её клиентам и выведет итоговый результат после сбора всех частей.

## 📐 Особенности реализации

- **Максимальная утилизация:** Клиент использует `std::thread::hardware_concurrency()` для автоматического определения оптимального количества вычислительных потоков.
- **Потокобезопасность:** Работа с сетью и консольный ввод разнесены по разным потокам. Синхронизация данных реализована через сигналы и слоты Qt (`QueuedConnection`), что исключает блокировки и гонки данных.
- **Сериализация:** Данные между программами передаются в бинарном сериализованном виде через `QDataStream`.
- **Логирование:** В обеих программах реализовано информативное логирование хода выполнения (подключения, получение задач, отправка результатов).

## 📂 Структура репозитория

- `Server/` — исходный код сервера, обработка сетевых соединений и распределение задач.
- `Client/` — код вычислительного узла и математический модуль `Integrator`.
- `CMakeLists.txt` — конфигурация сборки всего проекта.
- `Doxyfile` — файл конфигурации для генерации документации.

## 📝 Документация

Код полностью покрыт комментариями в стиле Doxygen. Чтобы сгенерировать HTML-документацию:

1. Установите **Doxygen**.
2. Выполните команду `doxygen Doxyfile` в корне проекта.
3. Откройте файл `html/index.html` в любом браузере.